# PCO-004 — cierre operativo y monitor de ventas trazable

## Alcance aprobado

Implementación y activación productiva autorizada de `PRD-FR-208` y `PRD-FR-218`:
apertura/cierre operativo idempotente, atribución del pago al turno de cobro, snapshots históricos,
monitor y drill-down, migración `0038`, contratos HTTP/POS y observabilidad. No incluye corte final,
contado/diferencia, reapertura, offline, reportes PCO-007, impresión ni exportación.

## Trazabilidad

- Requisitos: `PRD-FR-208`, `PRD-FR-218`.
- Diseño: SDD §38.2.3 y `SDD-ADR-026`.
- BDD: `BDD-SC-284`, `BDD-SC-285`, `BDD-SC-292`, `BDD-SC-307` a `BDD-SC-311`.
- TDD: `TDD-TS-080`, `TDD-TC-076`, `TDD-TC-090` a `TDD-TC-095`.

## Resultado implementado

- El cierre persiste `OPEN -> CLOSING -> OPERATIVELY_CLOSED` con un único artefacto
  `cash_shift_closures`; no escribe `cash_shift_cuts` ni `UserCashCut`.
- Pago y cierre usan el mismo guard. La carrera SQLite ejecutada confirma un único resultado:
  el pago confirmado se incluye en el snapshot, o el cierre gana y el pago falla sin residuos.
- El monitor proyecta pagos y líneas snapshot; filtrar por familia suma únicamente las líneas de la
  familia solicitada y no consulta el catálogo vivo. Montos incompletos permanecen separados como
  `unknown_operation_count`.
- Respuestas PCO serializan timestamps como RFC3339 UTC; la UI exige sucursal concreta con zona
  horaria válida para convertir el día local. Lista de turnos y drill-down usan límites `1..100` y
  cursores estables validados estrictamente.
- El preflight de `0038` rechaza moneda de pago/pedido ausente, no ISO-3 o incompatible. No inventa
  moneda ni crea snapshots ambiguos.
- Para historial `CONFIRMED`, el único alias adicional es `takeaway` exacto: `0038` lo proyecta como
  `takeout` sólo en el snapshot y deja el pedido sin cambios. Los comandos actuales siguen rechazando
  ese alias y cualquier otro tipo legacy bloquea el preflight.
- Métricas estructuradas locales: `cash_shift_open_total`,
  `cash_shift_operational_close_total`, `cash_shift_guard_conflict_total`,
  `sales_monitor_request_total` y `sales_monitor_incomplete_operations`. Sus campos no incluyen
  idempotency key, payload, filtros completos ni PII.

## Evidencia automatizada ejecutada

- `tests/architecture/test_traceability.py -q`: **8 passed**.
- `node tests/frontend/test_pos_operational_shift_sales_monitor.mjs`: exit 0. Es contrato estático
  de ruta/permiso, zona de sucursal, ausencia de cálculos TypeScript y reglas de contención
  responsive; no es una sesión visual de navegador.
- Suite API completa posterior a las correcciones: **182 passed, 10 skipped**.
- Suite raíz: **122 passed**.
- Suite PCO-004 afectada posterior al alias histórico: **46 passed, 2 skipped**.
- Auditoría independiente del alias: **76 passed, 1 skipped**; arquitectura y trazabilidad:
  **20 passed**. Los skips PostgreSQL destructivos requieren una base aislada `pco004_*`; nunca se
  ejecutaron contra producción.
- Roundtrip SQLite independiente `0037 -> 0038 -> 0037 -> 0038`: conserva
  `orders.order_type=takeaway`, produce un solo snapshot `takeout` y rechaza variantes desconocidas.
- CI de los PR #24 y #25: Python, frontend y Docker en verde. Typecheck y builds POS, Admin y KDS en
  verde con el runtime Node soportado del workspace.
- `git diff --check`: exit 0.

## Frontend y responsive

La prueba ejecutada verifica que `/sales-monitor` está protegido por `reports.sales.read`, que no
existe “Todas las autorizadas”, que la zona proviene de la sucursal seleccionada y que la tabla usa
scroll interno con reglas responsive. Queda pendiente QA visual real con fixtures deterministas a
1440x900 y 1000x800: foco/teclado, estados loading/error/vacío/datos, contención y ausencia de scroll
horizontal de página.

## PostgreSQL, canary y producción

- Snapshot restaurable de EasyPanel: `pre-pco004-2026-08-12` sobre `kiwi-postgres`; no se sustituyó
  ni reconfiguró la `DATABASE_URL` original.
- PR #24 desplegado; el primer `alembic upgrade 0038_cash_shift_closures_sales_monitor` falló cerrado
  antes del DDL al detectar dos pedidos confirmados con el código histórico exacto `takeaway`.
- Git histórico confirmó que versiones antiguas del POS emitían ese valor para “Para llevar”. PR #25
  documentó, probó, auditó y publicó la proyección limitada `takeaway -> takeout`, sin actualizar
  pedidos ni aceptar el alias en comandos nuevos.
- Redeploy del merge `9aa9eb7` finalizó `Success`; `alembic heads` mostró una sola head y el upgrade
  productivo avanzó de `0037_cash_movement_ledger` a
  `0038_cash_shift_closures_sales_monitor (head)`.
- Verificación agregada posterior: 6 snapshots de ventas; 2 fuentes históricas `takeaway` conservadas
  y 2 snapshots canónicos `takeout`; 0 cajas activas duplicadas.
- `/health/live` respondió `ok`; `/health/ready` respondió `ok` con PostgreSQL y Redis alcanzables.
- Canary vacío autorizado en una sucursal activa con caja exclusiva `QA-PCO004`: apertura y replay
  200 con el mismo turno; cierre y replay 200 con el mismo cierre; estado final
  `OPERATIVELY_CLOSED`; monitor y drill-down 200; 1 cierre operativo, 0 cortes finales y 0 turnos QA
  activos. No se creó ni cobró una venta.

Ante incidente, seguir la sección **PCO-004: cierre operativo y monitor de ventas** de
`docs/10-operacion-easypanel.md`: revertir primero la aplicación y conservar `0038`; no usar
`alembic stamp`, no editar historia y no forzar un downgrade cuando exista historia capturada.

## Riesgo residual

PCO-004 está activo en producción. Permanecen como evidencia no ejecutada la carrera concurrente y
el roundtrip destructivo sobre PostgreSQL **aislado** `pco004_*`; producción no debe usarse para esos
gates. También queda pendiente una sesión visual autenticada con fixtures deterministas a 1440x900 y
1000x800 para ampliar la evidencia responsive; los contratos estáticos, typecheck, builds, API real,
migración y canary productivo sí quedaron verificados. El corte final continúa fuera del alcance y
queda pendiente según `PRD-FR-219`.
