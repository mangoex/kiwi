# AUD-2026-09 — correcciones de auditoría

## Alcance y estado

R3: precisión monetaria, ingreso público y tipado de guardas/operaciones. El usuario autorizó
commit, integración y push después de recibir los límites de este reporte. El redeploy queda
a su cargo. Sin despliegue, migración ni modificación de datos históricos. No refactor masivo.
Facturación conserva comportamiento: sólo se almacena una lectura de configuración en una variable
para estrechar su tipo; no se implementó ni corrigió el módulo fiscal.

PRD conserva reglas; SDD precisa conversión exacta y lectura pública pura; BDD refuerza escenario
368; TDD incorpora regresiones y normaliza IDs 120–122 ya existentes como referencias alfanuméricas.
La matriz reubica FR-246..248. App.tsx/api.ts conservan clasificación de errores locales sin sugerir
WhatsApp ni propagar detalles libres; se retira exclusivamente el fallback no autorizado del carrito.

## Evidencia local

- RED: 5 regresiones monetarias/lectura pública fallaron y el carrito detectó el folio inventado;
  la nueva validación de secciones de matriz falló antes de reubicar requisitos.
- `pytest test_audit_money_public_regressions.py test_rappi_integration.py test_branch_reconciliation_reports.py`: 20 pasan.
- `pytest test_public_order_intents.py test_mobile_order_invariants.py test_public_order_rate_limit.py`: 35 pasan.
- `pytest tests/architecture`: 150 pasan.
- `pnpm test:frontend-semantic`: pasa incluyendo ocho archivos antes omitidos.
- `node tests/frontend/test_mobile_web_order_flow.mjs`: 13 pasan tras ajuste final de errores.
- `pnpm typecheck`: pasa. Builds admin, POS, KDS y móvil pasan; advertencias de tamaño de chunk existentes.
- `mypy apps/api/restaurant_os`: sin errores en 52 archivos (antes 110 errores).
- `ruff check apps/api tests`: pasa sin silenciamientos nuevos.
- Política de repositorio: pasa tras revisar excepciones sintéticas y renovar hashes de workflow
  y test_platform_api; no nuevas excepciones. `git diff --check`: pasa.
- QA Chrome local: catálogo desktop/mobile pasa; shell offline POS/KDS pasa; móvil con HTTP 503
  a 390 y 768 px conserva carrito, permite reintento y no fabrica referencias. Evidencia local
  `output/playwright/audit-mobile-390.png` y `audit-mobile-768.png` (ignorada).
- Tipado focal Terra: 89 pasan y 2 fallan; los mismos dos fallos se reprodujeron contra HEAD
  aislado en `../Kiwi-baseline-mypy-audit`: admin_ai vertical actions y onboarding confirm reciben
  `admin_catalog_corporate_scope_required`. No se relajaron permisos para obtener verde.

## Auditoría independiente Sol

- Afirmación: no se pierden centavos. Evidencia: Decimal/ROUND_HALF_UP; refutación con 19.99,
  0.29, 1.005 y amount en centavos; resultado verde. Riesgo: esto no certifica toda la conciliación.
- Afirmación: GET no crea claves. Evidencia: código de sólo lectura y prueba que prohíbe commit;
  refutación sucursal sin clave; resultado verde. Alta usa token_urlsafe en misma transacción,
  duplicado no agrega clave. Riesgo: claves históricas derivadas de UUID de migración 0062 permanecen.
- Afirmación: móvil no inventa éxito. Evidencia: 13 regresiones y QA 503 real en navegador;
  refutación error con detalle privado; sin eco ni WhatsApp alternativo, carrito conservado.
  Riesgo: no se simuló caída de conexión después del commit productivo.
- Afirmación: tipado preserva atomicidad. Evidencia: proxy mantiene commit→flush, cast es identidad,
  rowcount sigue fallo cerrado; revisión independiente sin defecto nuevo bloqueante del diff.

## Límites abiertos, sin certificación de release

1. Defecto contable PREEXISTENTE reproducido por Sol: fondo 200, compra pagada 19.99 y su movimiento
   PURCHASE producen supplier_expenses=19.99 y fixed_expenses=19.99, saldo 160.02 en vez de 180.01.
   También afecta diseño de compensaciones PURCHASE_CANCELLATION y fechas históricas. No modificar
   estados ni excluir movimientos arbitrariamente; requiere reparación contable dirigida con BDD/TDD.
2. Browser `test_admin_retro.mjs` falla en neutralidad RGB (17,24,39), exigida por PRD-FR-237/SDD §46.
   `test_admin_ai_assistant.mjs` espera encabezado “Productos y catálogo” ausente en diseño actual.
   Se consultó al usuario si conservar diseño actual o restaurar contrato monocromático. No se
   debilitaron aserciones ni se desactivaron pruebas. Las tres pruebas antes huérfanas quedan cableadas.
3. Dos pruebas backend anteriores fallan también en HEAD; no son regresiones del tipado, pero siguen
   siendo gates rojos. CI remoto y PostgreSQL no ejecutados; no hay Docker/psql local disponible.
4. No canary productivo ni despliegue. CI sólo podrá certificar el paquete cuando los gates rojos
   anteriores se resuelvan. No se agrega trigger push porque GOV-TEST-001 evita duplicar suite completa.

Preguntas operativas: ¿coincide importe normalizado/recibido? ¿hay resultado persistido tras rechazo?
¿la sucursal tiene clave configurada? Se responde con regresiones, command result/auditoría existente
y lectura de configuración; sin registrar PII ni secretos. Rollback de aplicación revierte código;
no hay migración ni rotación destructiva, y claves ya creadas se conservan.

Node mínimo 22.12 alineado con engines de Vite 7 instalado. El flag transform-types existe desde
22.7: https://nodejs.org/download/release/v22.14.0/docs/api/typescript.html .
