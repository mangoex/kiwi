# PCO-007 — recetas y proyecciones históricas

Alcance: `PRD-FR-220`, `TDD-TS-082` y `TDD-TC-121..128`.

## Evidencia local

Los gates focales cubrieron contrato HTTP, recetas versionadas, proyecciones de insumos/gastos,
migración SQLite, trazabilidad, Ruff, contratos semánticos y `git diff --check`. La QA visual fue
auditada a 1440x900 y 1000x800; incluyó las correcciones de legibilidad de tablas y la persistencia
del alcance corporativo explícito.

## Evidencia CI

PR #32, commit `06425da`: [run 31904296481](https://github.com/mangoex/kiwi/actions/runs/31904296481) terminó 4/4 jobs verdes. Python:
`442 passed, 12 skipped`; `apps/api/tests/test_pco007_postgres.py` ejecutó 6/6 contra la base aislada.
Ruff, contratos semánticos frontend, typecheck, builds, Docker y whitespace también quedaron verdes.

## Límites

No se usó `DATABASE_URL`, base productiva, deploy ni migración productiva. Merge y deploy requieren
autorización separada. El warning deprecado de Starlette observado en pruebas es residual no bloqueante.
